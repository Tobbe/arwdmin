import { Link, routes } from '@redwoodjs/router'

import { useAuth } from 'src/auth'

const ArwdminArwdminPage = () => {
  return (
    <>
      <h1>aRWdmin</h1>
      <Link to={routes.login()}>Log in</Link>
      <Link to={routes.signup()}>Sign up</Link>
    </>
  )
}

export default ArwdminArwdminPage
