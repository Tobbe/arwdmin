import New<%= model.pascalName %> from 'src/pages/Radmin/<%= model.pascalName %>/New<%= model.pascalName %>Page/New<%= model.pascalName %>'

const New<%= model.pascalName %>Page = () => {
  return <New<%= model.pascalName %> />
}

export default New<%= model.pascalName %>Page
